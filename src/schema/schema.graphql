# import Stats from './stats.graphql'
# import AssetPool from './asset-pool.graphql'
# import TransactionConnection from './transactions/connection.graphql'

type Query {
  stats: Stats!
  pools: [AssetPool]!
  transactions(limit: Int = 10, offset: Int = 0): TransactionConnection!
}
